meta {
  name: Update Reservation Status
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/reservations/1/status?status=CONFIRMED
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}