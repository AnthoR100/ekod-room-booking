meta {
  name: 03 - Refus sans token (401)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/reservations/me
  body: none
  auth: none
}

tests {
  test("status 401 sans token", function() {
    expect(res.status).to.equal(401);
  });
}
